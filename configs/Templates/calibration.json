{
    "Calibration": {
        "name": "calibration",
        "host": "localhost",
        "port": 7351,
        "modules":[{
            "name": "calibration",
            "type": "Calibration",
            "settings": {
                "configFile": "Module0.json",
                "testList": [2],
                "outBaseDir": ".",
                "verbose": 3,
                "l1delay": 130,
                "noRunNumber": true,
                "usb": true

            }
        }],
        "loglevel": { "$ref": "top.json#loglevel" },
	    "metrics_settings": { "$ref": "top.json#metrics_settings" }
    }
}